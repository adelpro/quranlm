# Use the classic node_modules linker so Vite + WASM-heavy deps like
# @litert-lm/core resolve predictably. (PnP works for Vite but trips
# over native bindings and postinstall scripts.)
nodeLinker: node-modules

enableGlobalCache: false